const cores = ['azul', 'vermelho', 'verde'];
const itens = cores.map( cor => `<li>${cor}</li>`);
console.log(itens);

/**
 * .map() - Calls a defined callback function on each element of an array, and returns an array that contains the results.
 * 
 */